# Test data - contains encrypted Signal backup (sensitive)
data/
.venv/

# Decrypted outputs (NEVER commit these!)
signal_key.txt
decrypted_*.json
signal_messages.json

# Python cache
__pycache__/
*.pyc
.pytest_cache/
